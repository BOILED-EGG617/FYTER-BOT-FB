# Copy this from .env.example -> .env and replace placeholders with real values
# KEEP THIS FILE SECRET. Do NOT commit to git.

# Facebook group ID (numeric)
FB_GROUP_ID=1331634114867492


# Long-lived access token (keep secret)
FB_ACCESS_TOKEN=EAABsbCS1iHgBAEXAMPLE_LONG_LIVED_TOKEN_REPLACE_ME

# Your Facebook numeric user id (only this id can issue admin commands)
ADMIN_FACEBOOK_ID=100000106021440

# Webhook verify token (choose any strong string — must match FB App webhook config)
FB_VERIFY_TOKEN=my_verify_token_Ravi9579464434

# Secret to protect /command endpoint (choose a strong random string)
ADMIN_COMMAND_SECRET=!

# Optional: Graph API base
GRAPH_API_BASE=https://graph.facebook.com/v17.0

# Flask settings (optional)
FLASK_ENV=production
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5000
